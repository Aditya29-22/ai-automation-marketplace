async function run() {
  const React = require('react');
  const ReactDOMServer = require('react-dom/server');
  
  // We need Babel/esbuild to compile the TSX...
  // Easier to use Vite's transform or tsx.
}
run();
